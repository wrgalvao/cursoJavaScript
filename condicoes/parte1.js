var velocidadePermitida = 60
var velocidade = 70
if(velocidade > 60)
{
    console.log(`Voce ultrapassou a velocidade permitida de ${velocidadePermitida} km/h você foi multado`)
    console.log('Dirija sempre utilizando o cinto de segunrança!')
}
else
{
    console.log(`Continue sua viagem tranquilamente e mantenha-se na velocidade maxima permitida de ${velocidadePermitida} km/h`)
}